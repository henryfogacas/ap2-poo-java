# ap2-poo-java

## 📝 Descrição da Atividade

O objetivo foi criar um sistema simples em Java para gerenciar veículos, implementando:

- Uma classe abstrata `Veiculo`.
- Classes concretas `Carro` e `Moto` que herdam de `Veiculo`.
- Implementação de métodos abstratos (`informacoesVeiculo()`).
- Uso de atributos públicos e privados com getters/setters.

## 📁 Estrutura dos Arquivos

- `Veiculo.java`: Classe abstrata base com atributos comuns e o método abstrato.
- `Carro.java`: Classe filha que representa um Carro.
- `Moto.java`: Classe filha que representa uma Moto.
- `Main.java`: Classe principal para instanciar e testar os objetos.

## 🚀 Como Executar

Para compilar e executar este projeto (requer JDK):

1.  Clone o repositório
2.  Navegue até a pasta:
    ```bash
    cd src
    ```
3.  Compile os arquivos Java:
    ```bash
    javac *.java
    ```
4.  Execute a classe Main:
    ```bash
    java Main
    ```
